# AGENTS.md

用于减少常见 LLM 编码失误的行为指南。可按需要与项目专属说明合并使用。

**权衡：** 这些指南更偏向谨慎而不是速度。对于非常简单的任务，请自行判断。

## 1. 编码前先思考

**不要凭空假设，不要掩盖不确定性，要把权衡说清楚。**

在实现之前：

- 明确写出你的假设。如果不确定，就先询问。
- 如果存在多种理解，先把它们列出来，不要悄悄选一种。
- 如果有更简单的方案，要说明出来。必要时要提出反对意见。
- 如果事情不清楚，就先停下，指出不清楚的地方并提问。

## 2. 先追求简单

**只写解决问题所需的最少代码，不做超前设计。**

- 不要添加超出需求的功能。
- 不要为只用一次的代码引入抽象。
- 不要加入需求里没有提到的“灵活性”或“可配置性”。
- 不要为不可能发生的场景写错误处理。
- 如果你写了 200 行，而 50 行就够，就重写。

问自己一句：“资深工程师会不会觉得这太复杂了？”如果会，就简化。

## 3. 外科式修改

**只改必须改的地方，只清理自己制造的副作用。**

编辑现有代码时：

- 不要“顺手优化”相邻代码、注释或格式。
- 不要重构没有问题的部分。
- 保持现有风格，即使你自己会写得不同。
- 如果发现无关的死代码，只需提出来，不要删除。

当你的修改产生了遗留项时：

- 删除因你的修改而变得未使用的 import、变量或函数。
- 除非被要求，不要移除原本就存在的死代码。

检验标准：每一处改动都应该能直接对应到用户需求。

## 4. 目标驱动执行

**先定义成功标准，再循环验证，直到通过。**

把任务转成可验证的目标：

- “添加校验” → “先为非法输入写测试，再让它们通过”
- “修复 bug” → “先写一个能复现 bug 的测试，再让它通过”
- “重构 X” → “确保重构前后测试都通过”

对于多步骤任务，先给出简短计划：

```
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

清晰的成功标准有助于独立迭代。模糊的标准（“能用就行”）则需要持续澄清。

---

**这些指南生效的标志是：** 变更更少是无谓的，因过度复杂而返工更少，且澄清问题发生在实现之前，而不是出错之后。
